context("dscore_vector")

items <- c("b3c31", "b3c32", "b3c33", "b3c34")
scores <- c(0, 1, 0, 1)
age <- rep(1, 4)

# d <- dscore_vector(scores = scores, items = items, ages = age,
#                    itembank = gcdg_itembank, lexicon = "gcdg")
# rm(d)
#
# # no tau found
# items <- c("b3c1", "b3c2", "b3c3", "b3c4")
# d <- dscore_vector(scores = scores, items = items, ages = age,
#                    itembank = gcdg_itembank, lexicon = "gcdg")
